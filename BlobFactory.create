seed = hash(racer.id + racer.name + colors)
baseRadius = 22..32 from seed
points = N=12 with angle step and radius variance per seed
store controlPoints + per-point wobblePhase
init eyes (blinkTimer), mouth orientation=pi

